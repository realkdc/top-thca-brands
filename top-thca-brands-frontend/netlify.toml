[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Serve SPA for all routes (React app handles routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200